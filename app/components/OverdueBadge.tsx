export default function OverdueBadge() {
  return (
    <span className="inline-flex items-center rounded-full bg-danger px-2 py-0.5 text-xs font-semibold text-danger-foreground ring-1 ring-danger/20">
      Overdue
    </span>
  );
}
