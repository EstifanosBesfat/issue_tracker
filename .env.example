# Database — prefer Neon pooled URL for cloud, or local Postgres for offline
# Neon (recommended):
# DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx-pooler.region.aws.neon.tech/neondb?sslmode=require
# Quick setup: ./scripts/use-neon.sh "<your-neon-url>"
# See docs/NEON.md
DATABASE_URL=postgresql://ethio:ethio@localhost:5432/project_manager?schema=public

# NestJS API
PORT=4000
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d
FRONTEND_URL=http://localhost:3000
APP_URL=http://localhost:3000

# Optional email (password reset)
RESEND_API_KEY=
EMAIL_FROM=onboarding@resend.dev

# Optional Cloudinary (image uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Next.js (apps/web/.env.local for local; apps/web/.env.production for Vercel builds)
NEXT_PUBLIC_API_URL=http://localhost:4000/api
# Production example:
# NEXT_PUBLIC_API_URL=https://teleprojectmanager-three.vercel.app/api
